{
  "name": "node-rotater",
  "version": "0.0.0",
  "description": "Emit rotate events for log files based on interval. Rotate event handlers are called \"rotater-tots\" or just tots. Tots get passed a readable stream the log file path and the data passed in when the log was asociated. ",
  "main": "index.js",
  "scripts": {
    "test": "jshint --config .lint.json ./index.js && tap ./test/*.js"
  },
  "repository": "",
  "keywords": [
    "logs",
    "rotate",
    "tater-tots"
  ],
  "devDependencies":{
    "tap":"*",
    "jshint":"*"
  },
  "author": "Ryan Day",
  "license": "BSD"
}
